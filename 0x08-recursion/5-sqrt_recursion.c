#include "main.h"
/**
 *
 * _qrt_recursion - func that reurns the squre root of a num
 * @n: integer
 * Return: 0
 */
int _sqrt_recursion(int n)
{
	in int x;

	if (n < 0)
		return (-1);
	if (x * x == n)
	{
		return (n, x + 1);
	}
	return (n, x + 1);
